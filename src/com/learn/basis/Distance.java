package com.learn.basis;

import java.util.HashMap;
import java.util.Scanner;

/**
 * Created by GIDEON on 2018/1/2.
 */
public class Distance {
    /*
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        HashMap<String,Integer> cities = new HashMap<>();
        String temp = null;
        int index = 0;
        while (true) {
            temp = scanner.next();
            if (temp.equals("###")) break;
            cities.put(temp,index);
            index ++;
        }

        int[][] distance = new int[index][index];
        for (int i = 0; i < index; i ++) {
            for (int j = 0; j < index; j ++) {
                distance[i][j] = scanner.nextInt();
            }
        }

        int key1 = cities.get(scanner.next());
        int key2 = cities.get(scanner.next());
        System.out.println(distance[key1][key2]);
    }
    */
}
